## 3D 几何构造工具 — 开发设计文档

---

### 一、项目概述

#### 1.1 目标

构建一个基于命令输入 + 鼠标交互的 3D 几何图形构造工具，用户可以通过命令精确建图，也可以通过鼠标拖拽微调。

#### 1.2 核心能力

```
命令输入建图    点/线/面/体的精确构造
拖拽微调        鼠标拖拽顶点，关联对象自动更新
翻折控制        绕轴任意角度旋转
样式控制        颜色/虚线/透明度
历史管理        撤销/重做
```

#### 1.3 技术栈

```
渲染引擎     Three.js（ES Module）
UI 框架      纯 HTML/CSS/JS（不引入前端框架）
构建工具     无（单 HTML 文件或多文件 ES Module）
外部依赖     Three.js 及其 addons（OrbitControls、CSS2DRenderer）
```

---

### 二、系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                         UI 层                                │
│  ┌──────────┐  ┌──────────────────┐  ┌────────────────────┐ │
│  │  工具栏   │  │    命令输入面板    │  │     属性面板       │ │
│  │          │  │                  │  │                    │ │
│  │ 🔘选择   │  │ > A=(0,0,0)      │  │ 选中: A            │ │
│  │ • 画点   │  │ > Segment(A,B)   │  │ 坐标: (0, 0, 0)   │ │
│  │ ╱ 画线   │  │ > Fold(P,L,90)   │  │ 颜色: [■]         │ │
│  │ △ 画面   │  │ > Undo           │  │ 标签: A            │ │
│  │ ✋ 拖拽   │  │ [执行] [清空]     │  │                    │ │
│  └──────────┘  └──────────────────┘  └────────────────────┘ │
├──────────────────────────────────────────────────────────────┤
│                      命令解析器                               │
│  ┌────────────┐  ┌────────────┐  ┌────────────────────────┐ │
│  │ Tokenizer  │→ │   Parser   │→ │       Executor         │ │
│  │  词法分析   │  │   语法解析  │  │  调用几何操作函数       │ │
│  └────────────┘  └────────────┘  └────────────────────────┘ │
├──────────────────────────────────────────────────────────────┤
│                      几何数据层                               │
│  ┌──────────────────────────────────────────────────────────┐│
│  │ ObjectStore（对象仓库）                                   ││
│  │  objects: Map<name, GeomObject>                          ││
│  │  history: Array<CommandRecord>   ← 撤销/重做栈           ││
│  │  dependencyGraph: DAG            ← 依赖关系图             ││
│  └──────────────────────────────────────────────────────────┘│
│  ┌──────────────────────────────────────────────────────────┐│
│  │ GeomObject 类型                                          ││
│  │  Point · Segment · Ray · Line · Triangle · Polygon       ││
│  │  Plane · FoldResult                                      ││
│  └──────────────────────────────────────────────────────────┘│
│  ┌──────────────────────────────────────────────────────────┐│
│  │ 几何计算函数                                              ││
│  │  midpoint · fold · reflect · project · distance          ││
│  └──────────────────────────────────────────────────────────┘│
├──────────────────────────────────────────────────────────────┤
│                      交互控制层                               │
│  ┌──────────┐  ┌───────────┐  ┌──────────┐  ┌────────────┐ │
│  │ 选择管理  │  │ 拖拽控制   │  │ 工具状态机│  │ 拾取(Ray)  │ │
│  └──────────┘  └───────────┘  └──────────┘  └────────────┘ │
├──────────────────────────────────────────────────────────────┤
│                     渲染层 (Three.js)                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐ │
│  │ 几何渲染  │  │ 标签渲染  │  │ 坐标轴   │  │ 网格/辅助  │ │
│  │ 点球/线   │  │ CSS2D    │  │ 箭头     │  │ 背景/雾效  │ │
│  │ 面/虚线   │  │ TextBlock│  │ 刻度     │  │            │ │
│  └──────────┘  └──────────┘  └──────────┘  └────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

---

### 三、目录结构

```
geometry-tool/
├── index.html                 入口页面 + UI 结构
├── style.css                  样式
│
├── parser/
│   ├── tokenizer.js           词法分析
│   ├── parser.js              语法解析
│   └── executor.js            命令执行
│
├── geometry/
│   ├── store.js               对象仓库 + 依赖图 + 历史栈
│   ├── types.js               GeomObject 类型定义
│   ├── calc.js                几何计算函数
│   └── constraints.js         约束系统（中点/等长/垂直）
│
├── interaction/
│   ├── toolManager.js         工具状态机
│   ├── picker.js              3D 拾取（Raycaster 封装）
│   ├── dragger.js             拖拽控制
│   └── selector.js            选中管理 + 高亮
│
├── render/
│   ├── scene.js               场景/相机/灯光初始化
│   ├── geomRenderer.js        几何对象 → Three.js Mesh/Line
│   ├── labelRenderer.js       标签渲染
│   ├── axesRenderer.js        坐标轴
│   └── gridRenderer.js        网格
│
└── main.js                    入口，组装各模块
```

---

### 四、数据结构设计

#### 4.1 GeomObject（几何对象基类）

```javascript
// 所有几何对象的通用结构
{
    name: 'A',                    // 对象名称（用户定义）
    type: 'point',                // 类型
    data: { ... },                // 类型特有数据
    style: {                      // 样式
        color: '#e0dcd2',
        opacity: 1.0,
        dash: false,
        visible: true,
        label: 'A'
    },
    parents: [],                  // 依赖的父对象名称列表
    renderRef: null               // 对应的 Three.js 对象引用（渲染层绑定）
}
```

#### 4.2 各类型 data 结构

```javascript
// 点
type: 'point'
data: { x: 0, y: 0, z: 0 }

// 线段
type: 'segment'
data: { from: 'A', to: 'B' }       // 引用点名

// 射线
type: 'ray'
data: { origin: 'A', through: 'B' }

// 无限直线
type: 'line'
data: { from: 'A', through: 'B' }

// 三角形
type: 'triangle'
data: { points: ['P', 'A', 'D'] }

// 多边形
type: 'polygon'
data: { points: ['A', 'B', 'C', 'D'] }

// 平面（方程定义）
type: 'plane'
data: { a: 0, b: 0, c: 1, d: 0 }          // ax + by + cz + d = 0
// 或
type: 'plane'
data: { points: ['A', 'B', 'C'] }          // 过三点

// 翻折结果（特殊类型，本质是一组新点）
type: 'fold'
data: {
    source: 'P',                              // 原始点
    axis: { from: 'A', to: 'D' },             // 旋转轴
    angle: 90,                                 // 角度
    result: 'P_prime'                          // 结果点名
}
```

#### 4.3 ObjectStore（对象仓库）

```javascript
{
    // 核心存储
    objects: Map<string, GeomObject>,
    
    // 历史栈
    undoStack: [],               // [{ type, name, snapshot }]
    redoStack: [],
    
    // 依赖图（邻接表）
    dependents: Map<string, Set<string>>,    // A → Set{'seg1', 'polygon1', 'E'}
    parents:    Map<string, Set<string>>,    // E → Set{'A', 'B'}
}
```

#### 4.4 依赖图操作

```javascript
// 注册对象时自动建立依赖
register('E', { type: 'midpoint', parents: ['A', 'B'] })
    → dependents.get('A').add('E')
    → dependents.get('B').add('E')
    → parents.get('E').set('A', 'B')

// 删除对象时断开依赖
unregister('E')
    → dependents.get('A').delete('E')
    → dependents.get('B').delete('E')
    → 同时删除 E 的渲染对象

// 拖拽 A 时，获取所有需要更新的对象
getAffected('A')
    → BFS/DFS 遍历依赖图
    → 返回 ['E', 'seg1', 'polygon1', ...]
    → 按拓扑序更新
```

---

### 五、命令解析器详细设计

#### 5.1 Tokenizer

**输入：** 字符串
**输出：** Token 数组

```
Token 类型：
    IDENT      标识符   [A-Za-z\u0370-\u03FF][A-Za-z0-9\u0370-\u03FF_]*
    NUMBER     数字     -?\d+(\.\d+)?
    LPAREN     (
    RPAREN     )
    COMMA      ,
    EQUALS     =
    PLUS       +
    MINUS      -
    STAR       *
    SLASH      /
```

**核心正则：**

```javascript
const TOKEN_RE = /\s*([A-Za-z\u0370-\u03FF_][A-Za-z0-9\u0370-\u03FF_']*|-?\d+\.?\d*|[=(),+\/*-])/g;
```

**特殊处理：**
- 跳过空白
- 支持 `P'` 中的撇号作为标识符一部分
- 支持 `α, β, θ` 等希腊字母作为变量名
- 支持负数 `-1.5`

#### 5.2 Parser

**输入：** Token 数组
**输出：** AST 节点

**AST 节点类型：**

```javascript
// 赋值语句
{ type: 'assign', name: 'A', value: ExprNode }

// 函数调用（无赋值）
{ type: 'call', func: 'Segment', args: [ExprNode, ...] }

// 无参命令
{ type: 'command', name: 'Undo' }

// --- 表达式节点 ---

// 标识符引用
{ type: 'ident', name: 'A' }

// 数字
{ type: 'number', value: 0 }

// 坐标元组
{ type: 'tuple', elements: [ExprNode, ExprNode, ExprNode] }

// 二元运算
{ type: 'binary', op: '+', left: ExprNode, right: ExprNode }

// 函数调用表达式
{ type: 'funcCall', func: 'Midpoint', args: [ExprNode, ...] }
```

**解析优先级（从低到高）：**

```
1. 赋值    A = expr
2. 加减    expr + expr, expr - expr
3. 乘除    expr * expr, expr / expr
4. 一元    -expr
5. 调用    Func(args...)
6. 原子    标识符 / 数字 / (元组) / (子表达式)
```

#### 5.3 Executor

**输入：** AST 节点
**输出：** 几何对象变更（创建/修改/删除）

```javascript
function execute(ast, store, renderer) {
    switch (ast.type) {
        case 'assign':
            executeAssign(ast, store, renderer);
            break;
        case 'call':
            executeCall(ast, store, renderer);
            break;
        case 'command':
            executeCommand(ast, store, renderer);
            break;
    }
}
```

**命令注册表（可扩展）：**

```javascript
const COMMANDS = {
    // 构造类
    'Point':    { minArgs: 1, maxArgs: 1, handler: cmdPoint },
    'Segment':  { minArgs: 2, maxArgs: 2, handler: cmdSegment },
    'Line':     { minArgs: 2, maxArgs: 2, handler: cmdLine },
    'Ray':      { minArgs: 2, maxArgs: 2, handler: cmdRay },
    'Triangle': { minArgs: 3, maxArgs: 3, handler: cmdTriangle },
    'Polygon':  { minArgs: 3, maxArgs: Infinity, handler: cmdPolygon },
    'Plane':    { minArgs: 1, maxArgs: 3, handler: cmdPlane },
    
    // 计算类
    'Midpoint': { minArgs: 2, maxArgs: 2, handler: cmdMidpoint },
    'Fold':     { minArgs: 3, maxArgs: 3, handler: cmdFold },
    'Reflect':  { minArgs: 2, maxArgs: 2, handler: cmdReflect },
    
    // 样式类
    'Color':    { minArgs: 2, maxArgs: 2, handler: cmdColor },
    'Dash':     { minArgs: 1, maxArgs: 1, handler: cmdDash },
    'Opacity':  { minArgs: 2, maxArgs: 2, handler: cmdOpacity },
    'Hide':     { minArgs: 1, maxArgs: 1, handler: cmdHide },
    'Show':     { minArgs: 1, maxArgs: 1, handler: cmdShow },
    'Label':    { minArgs: 2, maxArgs: 2, handler: cmdLabel },
    
    // 操作类
    'Delete':   { minArgs: 1, maxArgs: 1, handler: cmdDelete },
    'Undo':     { minArgs: 0, maxArgs: 0, handler: cmdUndo },
    'Redo':     { minArgs: 0, maxArgs: 0, handler: cmdRedo },
    'Clear':    { minArgs: 0, maxArgs: 0, handler: cmdClear },
    'Grid':     { minArgs: 0, maxArgs: 1, handler: cmdGrid },
    'Axis':     { minArgs: 0, maxArgs: 1, handler: cmdAxis },
};
```

**新增命令只需在 COMMANDS 中注册一行 + 写一个 handler 函数。**

---

### 六、交互系统设计

#### 6.1 工具状态机

```javascript
const ToolState = {
    IDLE:       'idle',        // 默认，可选择/拖拽
    PLACING_POINT: 'placing',  // 画点模式，点击放置
    DRAWING_LINE: 'drawing',   // 画线模式，依次选两点
    DRAWING_POLYGON: 'polygon' // 画面模式，依次选点，双击闭合
};
```

**状态转换：**

```
IDLE ──→ PLACING_POINT    （点击"画点"按钮）
IDLE ──→ DRAWING_LINE     （点击"画线"按钮）
IDLE ──→ DRAWING_POLYGON  （点击"画面"按钮）

PLACING_POINT  ──→ IDLE   （放置一个点后自动返回）
DRAWING_LINE   ──→ IDLE   （选完两点后自动返回）
DRAWING_POLYGON ──→ IDLE   （双击/右键闭合后返回）

任何状态 ──→ IDLE         （按 Esc 键取消）
```

**各状态下的鼠标行为：**

| 状态 | 左键点击空白 | 左键点击对象 | 右键 | 移动 |
|---|---|---|---|---|
| IDLE | 取消选择 | 选中对象 | 无 | 拖拽中则更新 |
| PLACING_POINT | 放置点 | 忽略 | 取消 | 显示预览点 |
| DRAWING_LINE | 忽略 | 记录端点 | 取消 | 显示预览线 |
| DRAWING_POLYGON | 忽略 | 记录顶点 | 闭合 | 显示预览边 |

#### 6.2 拖拽系统

```
mousedown
    ↓
Raycaster 检测是否命中可拖拽对象（点的包围球）
    ↓ 命中
记录拖拽目标 + 拖拽平面（通过该点的 XZ 平面）
    ↓
mousemove（拖拽中）
    ↓
Raycaster 与拖拽平面求交 → 计算新位置
    ↓
更新点坐标 → 触发依赖更新 → 重新渲染关联对象
    ↓
mouseup
    ↓
结束拖拽 → 记入历史栈
```

**拖拽平面选择：**

| 按键 | 拖拽平面 | 用途 |
|---|---|---|
| 无 | XZ 平面（y = 点的 y 值） | 默认，地面移动 |
| Shift | XY 平面（z = 点的 z 值） | 竖直面移动 |
| Ctrl | YZ 平面（x = 点的 x 值） | 竖直面移动 |

#### 6.3 选中与高亮

```javascript
// 选中对象后
selectedObject = obj;

// 高亮方式
- 点：球体变为亮色 + 放大 1.2 倍 + 外发光
- 线：变为亮色 + 线宽增加
- 面：变为亮色 + 边高亮

// 属性面板联动
- 右侧显示该对象的可编辑属性
- 修改属性后实时更新渲染
```

---

### 七、渲染层设计

#### 7.1 对象 → Three.js 映射

| GeomObject | Three.js 表示 |
|---|---|
| Point | SphereGeometry(r=0.06) + MeshStandardMaterial |
| Segment | BufferGeometry + LineBasicMaterial |
| Segment(dash) | BufferGeometry + LineDashedMaterial |
| Ray | 从 origin 向 through 方向延伸到视口边界 |
| Line | 从 through 方向两端延伸到视口边界 |
| Triangle | 自定义三角面 BufferGeometry + 透明材质 |
| Polygon | 三角化后自定义 BufferGeometry + 透明材质 |
| Plane | 大矩形面（可裁剪）+ 透明材质 |
| Label | CSS2DObject + TextBlock |

#### 7.2 渲染更新策略

```javascript
// 对象创建
function renderCreate(obj) → 创建 Three.js 对象并加入场景

// 对象更新（拖拽/命令修改后）
function renderUpdate(obj) → 更新 Three.js 对象的 geometry/position

// 对象删除
function renderRemove(obj) → 从场景移除并销毁 Three.js 对象

// 样式变更
function renderStyle(obj) → 更新 material 属性（颜色/透明度/虚线）
```

**关键：每个 GeomObject 保存一个 renderRef 指向对应的 Three.js 对象，更新时直接操作。**

#### 7.3 视觉风格

```css
:root {
    --bg:       #0b0b12;       深蓝黑背景
    --point:    #e0dcd2;       点的默认色
    --line:     #e0dcd2;       线的默认色
    --face:     #c9a04a;       面的默认色（金色）
    --accent:   #4aa0c9;       强调色（青蓝）
    --fold:     #9966cc;       翻折元素（紫色）
    --selected: #55ccee;       选中高亮
    --dash:     #3a3a4a;       虚线
    --grid:     #1a1a28;       网格线
}
```

---

### 八、撤销/重做系统

#### 8.1 命令记录

```javascript
// 每次命令执行后，记录到 undoStack
{
    type: 'create',            // 操作类型
    name: 'A',                 // 操作对象
    snapshot: {                // 快照（用于撤销）
        type: 'point',
        data: { x: 0, y: 0, z: 0 },
        style: { ... },
        parents: []
    }
}
// 或
{
    type: 'delete',
    name: 'A',
    snapshot: { ... }          // 被删前的快照
}
// 或
{
    type: 'modify',
    name: 'A',
    before: { x: 0, y: 0, z: 0 },
    after:  { x: 1, y: 0, z: 0 }
}
```

#### 8.2 撤销/重做逻辑

```javascript
function undo() {
    const record = undoStack.pop();
    switch (record.type) {
        case 'create':  // 撤销创建 = 删除
            deleteObject(record.name);
            break;
        case 'delete':  // 撤销删除 = 恢复
            restoreObject(record.name, record.snapshot);
            break;
        case 'modify':  // 撤销修改 = 恢复旧值
            updatePosition(record.name, record.before);
            break;
    }
    redoStack.push(record);
}

function redo() {
    const record = redoStack.pop();
    // 反向操作...
    undoStack.push(record);
}
```

---

### 九、UI 布局

```
┌──────────────────────────────────────────────────────────────┐
│  标题栏：3D 几何构造工具                                [—][□][×]│
├────────┬─────────────────────────────────────────┬───────────┤
│        │                                         │           │
│ 工具栏  │           3D 视口 (Three.js)             │  属性面板  │
│        │                                         │           │
│ [选择]  │                                         │  对象: A   │
│ [•点]  │        鼠标操作区域                       │  类型: 点  │
│ [╱线]  │        旋转/缩放/平移                     │  坐标:     │
│ [△面]  │                                         │  x: [  0 ]│
│ [✋拖]  │                                         │  y: [  0 ]│
│        │                                         │  z: [  0 ]│
│        │                                         │  颜色:[■]  │
│        │                                         │  标签:[A]  │
├────────┴─────────────────────────────────────────┴───────────┤
│  命令输入面板                                                  │
│  ┌─────────────────────────────────────────────┐ ┌────┐     │
│  │ > A = (0, 0, 0)                             │ │执行 │     │
│  │ > Segment(A, B)        历史记录区域           │ └────┘     │
│  │ > Fold(P, Line(A,D), 90)  ↑ 上键翻阅历史    │ [清空]      │
│  └─────────────────────────────────────────────┘            │
│  快捷提示: Enter执行 · ↑↓翻阅历史 · Tab补全 · Esc取消         │
└──────────────────────────────────────────────────────────────┘
```

**布局比例：**
- 工具栏：左侧 48px 固定
- 属性面板：右侧 220px 固定
- 3D 视口：中间自适应
- 命令面板：底部 140px 固定

---

### 十、完整命令语法规范

```
═══════════════════════════════════════════
  几何构造命令语法 v1.0
═══════════════════════════════════════════

── 构造类 ──

点:
  A = (x, y, z)                    绝对坐标
  A = Point(x, y, z)               同上
  A = B + (dx, dy, dz)             向量偏移
  A = B + C                        向量加法

线:
  Segment(A, B)                    线段
  Segment(A, B)                    命名: seg1 = Segment(A,B)
  Line(A, B)                       无限直线
  Ray(A, B)                        射线(A为端点，方向AB)

面:
  Triangle(A, B, C)                三角形
  Polygon(A, B, C, D, ...)         多边形（≥3点）
  Plane(A, B, C)                   过三点的平面
  Plane(a, b, c, d)                方程 ax+by+cz+d=0

── 计算类 ──

  E = Midpoint(A, B)               中点
  P' = Fold(P, Line(A,D), θ)       绕线旋转 θ°
  P' = Fold(P, Plane(A,B,C), θ)    绕面旋转 θ°
  P' = Reflect(P, Line(A,D))       关于线对称(=Fold…,180)
  P' = Reflect(P, Plane(A,B,C))   关于面对称
  d = Distance(A, B)               距离（显示用）

── 样式类 ──

  Color(obj, red)                  颜色（关键字）
  Color(obj, #ff8800)              颜色（十六进制）
  Color(obj, rgb(255,128,0))       颜色（RGB）
  Dash(obj)                        设为虚线
  Dash(obj, false)                 取消虚线
  Opacity(obj, 0.4)                透明度
  Hide(obj)                        隐藏
  Show(obj)                        显示
  Label(obj, "P₁")                 自定义标签

── 操作类 ──

  Delete(obj)                      删除对象
  Undo                             撤销
  Redo                             重做
  Clear                            清空所有
  Grid                             切换网格
  Axis                             切换坐标轴

── 注释 ──

  # 这是注释
  // 这也是注释
```

---

### 十一、开发阶段

#### 阶段一：骨架（3-4 天）

```
[ ] HTML/CSS 布局（三栏 + 命令面板）
[ ] Three.js 场景初始化（相机、灯光、网格、坐标轴）
[ ] Tokenizer 实现 + 测试
[ ] Parser 实现 + 测试
[ ] Executor 骨架（Command 注册表）
[ ] ObjectStore 基础（增删查改）
[ ] 仅支持：Point、Segment、Triangle、Polygon
    → 输入 10 行命令能看到四棱锥
```

#### 阶段二：交互（3-4 天）

```
[ ] Raycaster 拾取封装
[ ] 选择管理（点击选中 + 高亮）
[ ] 拖拽系统（拖拽点 + 依赖更新）
[ ] 工具状态机（点/线/面工具栏）
[ ] 属性面板（选中后显示/编辑属性）
    → 可以鼠标拖拽顶点，看到图形实时变化
```

#### 阶段三：进阶命令（2-3 天）

```
[ ] Fold / Reflect 实现
[ ] Midpoint 实现
[ ] Color / Dash / Opacity / Hide / Show 实现
[ ] Line / Ray / Plane 渲染
[ ] 数学表达式求值（加减乘除、变量引用）
    → 翻折、样式控制全部可用
```

#### 阶段四：完善（2-3 天）

```
[ ] Undo / Redo
[ ] 命令历史（上下键翻阅）
[ ] Tab 补全（对象名/命令名）
[ ] Delete + 级联删除（删除点时提示关联对象）
[ ] 错误提示（中文，指向错误位置）
[ ] 导出 PNG
```

---

### 十二、关键技术风险

| 风险 | 影响 | 应对 |
|---|---|---|
| 3D 拾取精度 | 点太小难以点中 | 点的包围球半径设大（0.15），线加辅助碰撞体 |
| 拖拽平面判断 | 拖拽时点飘到奇怪位置 | 默认 XZ 平面 + Shift 切换，始终显示拖拽平面辅助线 |
| 依赖更新顺序 | 环形依赖导致死循环 | DAG 严格无环 + 拓扑排序 + 更新深度上限 |
| 多边形三角化 | 凹多边形渲染错误 | 采用 Ear-cut 算法（Three.js 自带 ShapeGeometry 可用） |
| 表达式解析 | 嵌套运算优先级错误 | 递归下降解析器严格按优先级处理 |
| 大量对象性能 | 几百个对象拖拽卡顿 | 合并静态对象 geometry + 只更新变化对象 |

---

以上是完整的开发设计。下一步是否开始写阶段一的代码？